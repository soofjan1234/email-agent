# SMB share permission checks

If Windows clients cannot open an SMB share, confirm the shared folder permission, the user group, and that SMB service is enabled. A successful DSM login does not guarantee SMB access.
